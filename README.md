# API de Processamento de Excel

Esta API Flask processa arquivos Excel, preenche um modelo (`modelocroqui.xlsx`) e devolve o arquivo processado.

## Como usar

1. Instale as dependências:
   ```bash
   pip install -r requirements.txt